<h1>Plant Calendar</h1>

<div id="plant_watering">
	<%= calendar do |date| %>
		<%= date.day %>
		<% if @personal_plants_by_date[date] %>
			<% @personal_plants_by_date[date].each do |water| %>
				<ul>
					<li><%= water[:id] %></li>
				</ul>
			<% end %>
		<% end %>
	<% end %>
</div>